Here are the three exact replacements:
drawing_overlay.py — mouseReleaseEvent, stroke append:
pythonself.primitives.append({
    "kind":    "stroke",
    "points":  normalized,
    "width":   self.brush_size / max(1, self.width()),
    "color":   self._color_to_tuple(self.color),
    "opacity": self.brush_opacity,
})
drawing_overlay.py — _paint_primitives, committed stroke QPen:
pythonpen = QPen(QColor(*prim.get("color", (0, 0, 0))),
           prim.get("width", 0.005) * w,
           Qt.SolidLine, Qt.RoundCap, Qt.RoundJoin)
(Default 0.005 ≈ a sensible fallback in case an old primitive sneaks in with no width.)
pdf_viewer.py — overlay_render, stroke width calculation:
pythonstroke_width = float(prim.get("width", 0.005)) * pdf_w
