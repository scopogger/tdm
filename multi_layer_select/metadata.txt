[general]
name=Select Across Layers
qgisMinimumVersion=3.28
description=Highlight several layers in the Layers panel, then draw one rectangle on the map to select matching features on all of them at once
version=0.1.0
author=Your Name
email=you@example.com

about=QGIS's native selection tools only ever act on the single active layer. This tool reads whichever layers are highlighted in the Layers panel (the normal Ctrl/Shift-click multi-select QGIS already supports there) and applies one rectangle drag to all of them at once. Shift adds to each layer's existing selection, Ctrl removes from it, matching the native tools' conventions. Falls back to the active layer if nothing is highlighted in the panel.

tracker=
repository=
hasProcessingProvider=no
tags=vector,selection,utility,layers
category=Vector
experimental=True
deprecated=False
server=False
